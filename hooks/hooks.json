{
  "description": "SessionStart hook - checks availability of AI CLI tools (gemini, agy, codex, grok, claude)",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/session-start.sh",
            "statusMessage": "Checking AI CLI availability..."
          }
        ]
      }
    ]
  }
}
