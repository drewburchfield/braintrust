{
  "description": "SessionStart hook - checks availability of AI CLI tools (agy, codex, grok, opencode, claude)",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/session-start.sh",
            "statusMessage": "Checking AI CLI availability..."
          }
        ]
      }
    ]
  }
}
